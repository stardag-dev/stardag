services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: stardag
      POSTGRES_PASSWORD: stardag
      POSTGRES_DB: stardag
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U stardag"]
      interval: 5s
      timeout: 5s
      retries: 5

  migrations:
    build:
      context: ./app/stardag-api
      dockerfile: Dockerfile
    environment:
      STARDAG_API_DATABASE_URL: postgresql+asyncpg://stardag:stardag@db:5432/stardag
    command: ["alembic", "upgrade", "head"]
    depends_on:
      db:
        condition: service_healthy

  api:
    build:
      context: ./app/stardag-api
      dockerfile: Dockerfile
    environment:
      STARDAG_API_DATABASE_URL: postgresql+asyncpg://stardag:stardag@db:5432/stardag
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
      migrations:
        condition: service_completed_successfully

  ui:
    build:
      context: ./app/stardag-ui
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    depends_on:
      - api

volumes:
  postgres_data:
