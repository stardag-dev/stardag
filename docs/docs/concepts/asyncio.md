# AsyncIO

Stardag natively support `async` implementation of `Task`s and `Target`s.

---

!!! info "🚧 **Work in progress** 🚧"

    This documentation is still taking shape. It should soon cover:

    - How to leverage async python via `run_aio`, `complete_aio` etc.
    - FileSystemTargets have async implementation by default giving significant speed up in many scenarios
