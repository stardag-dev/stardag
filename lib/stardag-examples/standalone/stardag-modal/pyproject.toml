[project]
name = "stardag_modal"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["stardag[modal]>=0.27.0rc1", "modal"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
