[project]
name = "stardag_modal"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["stardag[modal]", "modal"]

[tool.uv.sources]
stardag = { path = "../../../stardag", editable = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
